import React from "react";

const ExportWindow = () => {
  return (
    <div>
      <button>Export JSON</button>
      <button>Export XML</button>
      <button>Backup all projects</button>
    </div>
  );
};

export default ExportWindow;
